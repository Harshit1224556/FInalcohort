how javascript run 

there is call stack terms 
means there is kind of manager for the functions


global execution context means where the
// code runs

//local execution context where function call

for every context there is two kind of things

// execution where execution happens
// memory where you can assign the memory to the variable
<!-- 
node js neiter a framework neither a library -->


<!-- Hoisting is a javascript mechanism where variable,
function, and class dedaratians are conceptually
moved to the top of their contaning scope
(script ar fanction) during the compilation phase,
before code execation. 1
- This allacos funations to be used before they
are defined and allocos variable to be accessed
withoat thrown error, though var variable retarn
undefined -->



age= 45;
Console.log (. Age is', age;
Var age -32;
hello ();
Console. log C'Age is', age;
Fanction hello c)S
Console.log (This is hello');

output is age is 45
this is hello
age is 32


this happens because there is memory score and code scope 

in the memory code data is filled


Var is not black scoped, when you dedared inside loopsy if
sHatement not. comfirred bort can be accessed. autside it
which con lead to the bay.


in the const object you can manipulate the data

in the const you can manipulate the content but cannot change the reference
you can test it in the object or array

type of null is object that is the major bug in the javascript

in the javascript undefined its been declared but not assinged a value
but in the null means internionally absent marked

primitve data type m cheejein call by value  hota hah 
non primitve data type m cheejein call by reference k through hota haha









