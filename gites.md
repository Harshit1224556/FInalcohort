git is used to track the changes
github is used to store the file in the cloud or kind of server which host the git same use can use it for git
repository is the collection of files and folder
pwd current working directory
ls is used to lsit all the files in the directory
Mercurial (Hg) is kind of git'
the moment when we git init it initialise git

ls -Force

git add filename means yaad kr lo 
stagging area m

git commit means save kr lo and saath m kuch msg krdoo


git diff is used to track the changes in the commit of the github

git status 