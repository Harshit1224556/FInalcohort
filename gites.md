git is used to track the changes
github is used to store the file in the cloud or kind of server which host the git same use can use it for git
repository is the collection of files and folder
pwd current working directory
ls is used to lsit all the files in the directory
Mercurial (Hg) is kind of git'
the moment when we git init it initialise git
ls -Force
git add filename means yaad kr lo 
stagging area m
git commit means save kr lo and saath m kuch msg krdo
git diff is used to track the changes in the commit of the githu
git log help to track about the commit history
git status helps to commited files and 
git uses  kind of linkedlist to go back to the old
changes and then revert to the old changes 
git log gives uu big output but 
you if you need sorter output you can go for the
git log  --oneline

lets take a activity where u push a bug accidently
now its turn how to revert the changes
git revert use kr lenge suppose jo usme use kiya hoga wo add kr lenge
git reset us commit history ko gayab kr deta hah 
git branch help us to identify which branch you are working on
basically git branch help me to understand this

intenally it read the head file of the .git file


git commit -am "msg" it modify the next file only


suppose hume ek git branch banana h and usme shift krna h to ky krenge
so there is command called git branch

suppose you want to make a branch and want to shift this to new branch
in this case

so git checkout -b branchname

for shift only git checkout branch name

git merge branchname is used to merge those provided branch with the current
branch


Git Squash is the process of combining multiple commits into a single commit to keep the project history clean and organized. It is commonly done using git rebase -i or git merge --squash.